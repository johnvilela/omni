---
tags: [session, omni]
lastUsed: 2026-09-01
---

# Omni MVP build (2026-09-01, 14:11–16:37)

Built Omni's first milestone from scratch: a Go server that echoes Telegram messages reversed, plus the `omni` CLI (charmbracelet bubbletea). TDD throughout — every unit's test written and watched fail before implementation. Verified end-to-end against a fake Telegram API (`OMNI_TELEGRAM_API` env override) in a scratchpad: connect via config token, incoming "hello omni 👋" answered "👋 inmo olleh", server restart auto-reconnects, friendly error when the server is down. `go test ./...` green across reverse/store/config/poller/API/client/TUI/routing.

Dependencies added and reasoned about, recorded in [[dependencies]]: `modernc.org/sqlite`, `gopkg.in/yaml.v3`, the three charm packages (`bubbletea`/`bubbles`/`lipgloss`). No Telegram bot library — three Bot API methods hand-rolled in ~120 stdlib lines (upgrade path noted for later: `go-telegram/bot`; never `go-telegram-bot-api`, dead since ~2021).

Committed as 5 focused commits at the user's request ("divide it into smaller commits"): `6e908fa` (agent instructions + wiki scaffolding), `35e8ff9` (go module + deps), `b0bf66c` (server), `4545cc3` (cli), `fd5867e` (wiki docs). `.memoria/` added to `.gitignore`.

Then built the four lifecycle scripts (`build.sh`, `install.sh`, `dev.sh`, `uninstall.sh`) — layout documented in [[install]]. Dev/prod coexistence solved via compile-time identity (`-ldflags -X main.app=... -X main.defaultAddr=... -X main.version=...`): same code, different compiled name/port/config/data dirs. Verified live: prod service active on :8787, dev server on :8788 simultaneously, `uninstall.sh --dev` removing only dev, full uninstall + reinstall. Committed as `82e296d`, `791becc`, `6946b31`.

Finally, reworked CLI help to be layered per command (`omni --help` → root commands only, no subcommand detail; `omni channels --help` → channel subcommands/examples; `omni channels connect --help` → the `-c` flag) — current shape documented in [[cli]]. Committed as `e5ec1b6`.
